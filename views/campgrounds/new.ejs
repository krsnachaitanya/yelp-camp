<%- include('../partials/header') %>

<div class="container">
  <h1 class="text-center">Create a New Campground</h1>

  <div class="col-md-6 mx-auto">
    <!-- Posts data to campgrounds page -->
    <form action="/campgrounds" method="post">
      <div class="form-group mt-4">
        <label for="name">Name</label>
        <input class="form-control" type="text" name="name" placeholder="Name" required>
      </div>
      <div class="form-group">
        <label for="cost">Price</label>
        <input class="form-control" type="number" name="price" placeholder="Price" min="1" step="1" required>
      </div>
      <div class="form-group">
        <label for="image">Image Url</label>
        <input class="form-control" type="text" name="image" placeholder="Image URL" required>
      </div>
      <iframe class="mapview__container" src="/campgrounds/mapform" name="mapform" frameborder="0"></iframe>
      <div class="form-group">
        <label for="location">Location</label>
        <input class="form-control" type="text" name="location" id="location" placeholder="Enter a location in the map"
          value="" required>
      </div>
      <div class="form-group">
        <label for="latitude">Latitude</label>
        <input class="form-control" type="text" name="latitude" id="latitude" placeholder="Enter a location in the map"
          value="" required>
      </div>
      <div class="form-group">
        <label for="longitude">Longitude</label>
        <input class="form-control" type="text" name="longitude" id="longitude"
          placeholder="Enter a location in the map" value="" required>
      </div>
      <div class="form-group">
        <label for="description">Description</label>
        <input class="form-control" type="text" name="description" placeholder="Description" required>
      </div>
      <div class="form-group">
        <button class="btn btn-lg btn-primary btn-block" type="submit">Submit!</button>
      </div>
    </form>
    <a href="/campgrounds">View All Campgrounds</a>
  </div>
</div>

<script>
  window.addEventListener('message', function (event) {
    var location = document.getElementById('location');
    var latitude = document.getElementById('latitude');
    var longitude = document.getElementById('longitude');
    if (Boolean(event.data.placename)) {
      location.value = event.data.placename;
      latitude.value = event.data.latitude;
      longitude.value = event.data.longitude;
    }
  });
</script>

<%- include('../partials/footer') %>